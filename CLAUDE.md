# GVIDON TOUR — общий контекст проекта

> **Этот файл — общая память проекта для всех, кто над ним работает (Claude, ChatGPT, человек).**
> Claude Code читает его автоматически в начале каждой сессии. Если работает другой ассистент —
> прочитать этот файл **до** любых правок и **дописать сюда** всё, что сделано и что выяснено.
> Правила: не удалять чужие записи; описывать не только «что сделал», но и «почему» и «что проверено»;
> перед началом работы — `git pull`, после — `git push` (иначе второй ассистент затрёт правки).
> Свежее состояние всегда: `git log --oneline -15`.
>
> **Единственный файл общей памяти проекта — `CLAUDE.md`.** Claude и ChatGPT работают по очереди и обязаны читать и дополнять именно его. `AGENTS.md` не использовать и не создавать: параллельные файлы памяти приводят к расхождениям контекста.

Сайт туроператора по Казахстану (Астана): публичная витрина + самописная админка.
Прод: https://gvidon-tour.vercel.app (Vercel, деплой автоматом при push в `main`).

## Общение и рабочий процесс

- **Пользователь не знает английский — все ответы в чате только по-русски.**
- Работа идёт этапами: построить этап → проверить вживую в браузере (и админку, и что изменения видны на публичном сайте) → закоммитить и запушить → отчитаться в чате → ждать «продолжай». Не начинать следующий этап без сигнала.
- Секреты: пользователь сам вносит переменные в Vercel Dashboard вручную — я только говорю имя и значение. В `.env` локально записываю сам. Значения секретов никогда не коммитить.
- Коммиты делать через **Bash tool + heredoc** (PowerShell портит кириллицу в сообщениях коммитов). В конце сообщения — Co-Authored-By.

## Стек

- Next.js 16 (App Router), React 19, TypeScript, Tailwind v4, lucide-react
- Prisma 7 + `@prisma/adapter-pg`, Postgres на Supabase (только БД и Storage; **Supabase Auth НЕ используется**)
- Клиент Prisma генерируется в `src/generated/prisma/client`, импорт `@/generated/prisma/client`
- Авторизация полностью своя: `jose` (JWT HS256, httpOnly-кука `gv_admin`, 7 дней) + `bcryptjs`
- В Next.js 16 middleware переименован: файл `src/proxy.ts` (default export + `config.matcher`)

Решение по архитектуре (подтверждено с пользователем): полный self-hosted Supabase на VPS **не нужен**. При переезде на VPS — обычный Postgres + MinIO/диск. Вся работа с хранилищем изолирована в одном модуле `src/lib/storage.ts` именно ради этого.

## Языки в URL (важно)

Язык — часть адреса: `/ru/…`, `/en/…`, `/kk/…` (все три с префиксом). Это сделано ради SEO: раньше язык жил в localStorage, сервер всегда отдавал русский HTML, и англ./каз. версии были невидимы для Google.

- `/` и любой старый адрес без префикса (`/tours`, `/blog/x`) → 307-редирект на `/ru/…` (в `src/proxy.ts`).
- Префикс казахского — `kk` (код языка по ISO 639-1). `kz` — это код страны, для hreflang он невалиден.
- Язык приходит из сегмента `[lang]` в `LanguageProvider` (см. `(site)/[lang]/layout.tsx`), а не из localStorage. `setLang` больше нет — смена языка это переход по ссылке (`LangSwitcher`).
- **Ссылки внутри сайта пишутся без префикса** (`href="/tours"`): их подставляет `src/components/LocaleLink.tsx`. В site-компонентах импортировать `Link` из `@/components/LocaleLink`, а не из `next/link`.
- Сравнение путей (`usePathname`) — только через `stripLocale()` из `src/lib/i18n.ts`, иначе сломается на `/en/...`.
- `src/lib/i18n.ts` не должен импортировать `content.ts` как значение (только `import type`) — его тянет edge-middleware, и словарь раздует бандл. `LANGS` в content.ts — это ре-экспорт `LOCALES` из i18n.
- API-роуты (`/api/…`) вне локали: POST нельзя редиректить, тело запроса потеряется. Форма отзывов шлёт на `/api/reviews/submit`.

## Страницы 404 и ошибок (важная находка)

**В этом проекте Next.js не рендерит 404 на сервере вообще.** Ответ 404 — это пустая оболочка (~1.9 КБ), содержимое дорисовывает браузер из RSC-payload. Проверено эмпирически: даже примитивный статический компонент без логики не попадает в HTML. Это поведение Next.js, а не следствие нашего кода — **не пытаться «чинить» это заново**. Обычные страницы при этом рендерятся сервером нормально (50–86 КБ разметки) — SEO не задет.

**Next.js игнорирует вложенный `not-found.tsx` под динамическим сегментом.** Проверены оба размещения — `(site)/[lang]/not-found.tsx` и прямо в `tours/[slug]/` — ни одно не срабатывает, сборка регистрирует единственный маршрут `/_not-found`. Поэтому 404 живёт только в корневом `src/app/not-found.tsx`.

Корневая 404 находится **выше** сегмента `[lang]` и не может взять язык из params. Раньше `proxy.ts` передавал язык заголовком, а `not-found.tsx` читал `headers()`. Это оказалось критической ошибкой: общая 404 входит в дерево всех страниц, поэтому `headers()` делал **весь публичный сайт динамическим** и отключал CDN/ISR. С 2026-07-15 оболочка 404 статическая, а `src/components/NotFoundContent.tsx` определяет язык из `usePathname()`; `proxy.ts` больше не клонирует заголовки. Проверено: `○ /_not-found`, все публичные маршруты `● SSG`, английская 404 отображается на английском. У корневого `<html>` 404 до гидратации остаётся `lang="ru"`; страница 404 не индексируется, это сознательный компромисс ради кэша всего сайта.

`error.tsx` — другой механизм (React error boundary), он **работает** во вложенных сегментах: `(site)/[lang]/error.tsx` и `admin/(panel)/error.tsx`. При серверной ошибке HTML тоже приходит оболочкой, а граница рисуется после гидратации — это нормально.

## Структура

- `src/app/(site)/[lang]/…` — публичные страницы (общий layout с Header/Footer/FloatingCta + LanguageProvider)
- `src/app/admin/login/…` — логин (вне panel-группы)
- `src/app/admin/(panel)/…` — админка: layout с сайдбаром, проверкой сессии и ToastProvider; разделы: обзор, requests, media, tours, directions (города), categories, gallery, reviews, blog, achievements. Уведомления об успехе — `useToast()` из `src/components/admin/Toast.tsx`
- `src/app/api/…` — endpoint'ы вне локали (`/api/reviews/submit`)
- `src/lib/auth.ts` — createSession / getSession / destroySession
- `src/lib/db.ts` — prisma-клиент
- `src/lib/storage.ts` — Supabase Storage через чистый REST (без SDK): upload/delete/list/publicMediaUrl
- `src/lib/telegram.ts` — уведомления о заявках (fail-soft, таймаут 5с)
- `src/lib/catalog.ts` — все геттеры каталога (`getTours`, `getReviews`, `getBlogPosts`, `getGalleryItems`, `getAchievements`); DB-результаты кэшируются на 5 минут через `unstable_cache`, вызовы внутри одного рендера дедуплицируются через React `cache()`
- `src/lib/catalog-cache.ts` — единые теги каталога для кэша и админской инвалидизации
- `src/lib/rate-limit.ts` — распределённые лимиты запросов через таблицу Postgres `RateLimit` (не `Map` памяти serverless-инстанса)
- `src/lib/a11y.ts` — локализованные RU/EN/KK подписи для скрытых accessibility-элементов и интерактивных контролов
- `src/components/Breadcrumbs.tsx` — видимая локализованная навигационная цепочка для туров/статей; JSON-LD строится отдельно через `breadcrumbJsonLd()` из `seo.ts`
- `src/lib/content.ts` — статические тексты RU/EN/KK (и fallback-данные)
- `src/components/admin/AdminFormFields.tsx` — общие поля форм админки: `emptyLText`, `LTextField`, `LTextListEditor`
- `scripts/create-admin.ts` — создать/обновить админа: `npx tsx scripts/create-admin.ts <login> <password> [name]`
- `scripts/create-bucket.ts` — создание bucket `media` прямым SQL в `storage.buckets`

Модели БД: Tour, Category, Direction, Review, GalleryItem, Achievement, Lead(+LeadStatus), AdminUser, BlogPost, RateLimit.

## Ключевые паттерны (обязательны к соблюдению)

1. **Загрузка файлов — ТОЛЬКО через Route Handler, никогда через Server Action.**
   Server Action с `<input type="file">` (multipart) в этом проекте вызывает баг: при построении ответа action Next.js перерисовывает layout-дерево, и `redirect("/admin/login")` из `(panel)/layout.tsx` срабатывает до выполнения самого action → 303 на логин. Долго дебажилось на этапе 3. Рабочее решение: `src/app/admin/(panel)/media/upload/route.ts` + ручной `fetch()` c FormData на клиенте + `router.refresh()`.
2. **Сложные формы админки вызывают server actions как RPC** — прямой вызов `await saveX(id, payload)` из клиентского кода, НЕ привязка к нативной `<form action={...}>`. Это обходит весь класс бага из п.1.
3. **Fallback на статику — только при СБОЕ БД, не при пустой таблице.** Геттер в `catalog.ts` ловит исключение → отдаёт демо из `content.ts` (сайт не белеет при аварии). Пустая таблица = админ всё снял/удалил → отдаём пусто. Раньше было `rows.length === 0 → демо`: админ прятал последний тур, и на сайте воскресали 6 демо-туров. **Не возвращать это обратно.** Блоки главной сами прячутся, когда данных нет.
4. **«Полусмигрированная фича» — главная ловушка проекта, наступали дважды.** Наличие таблицы, поля и CRUD НЕ значит, что сайт это читает. Было с Achievements (CRUD в пустоту) и с `featured` у туров (звёздочка в админке кликалась и сохранялась, а главная брала `slice(0,3)` — поля не было ни в `CatalogTour`, ни в маппинге `catalog.ts`). **При любой новой фиче: grep поля по `catalog-types.ts` + `catalog.ts` + компоненту сайта, и проверить сменой значения, что вид реально меняется** (совпадение с дефолтным порядком — не доказательство).
5. После мутаций в админке — `updateTag()` соответствующего тега из `CATALOG_TAGS`; `revalidatePath()` оставлять для динамических страниц самой админки. Не инвалидировать старые пути `/tours`, `/blog` и т. п.: публичные адреса локализованы, а тег автоматически сбрасывает все RU/EN/KK страницы, которые использовали эти данные.
6. Трёхъязычный контент: `LText = Record<"ru"|"en"|"kk", string>`, хранится в JSON-полях.
7. Каждый server action админки в начале сам проверяет `getSession()` (не полагаться только на proxy/layout).
8. Слаги: транслитерация через `slugify()` (кнопка «Из названия» в формах туров и блога), уникальность — ловить `Prisma.PrismaClientKnownRequestError` с кодом `P2002`.

## Переменные окружения (все уже в локальном `.env`)

| Имя | Назначение |
|---|---|
| `DATABASE_URL` | Postgres, transaction pooler :6543 (рантайм) |
| `DIRECT_URL` | Postgres, session pooler :5432 (CLI: db push, seed — DDL не работает через :6543) |
| `AUTH_SECRET` | подпись JWT сессии админки |
| `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID` | уведомления о заявках |
| `SUPABASE_URL`, `SUPABASE_SERVICE_KEY` | Storage REST API |
| `NEXT_PUBLIC_YM_ID`, `NEXT_PUBLIC_GA_ID` | Яндекс.Метрика и GA4 (`src/components/Analytics.tsx`). Не заданы → счётчики не грузятся. Публичные ID, не секреты. `NEXT_PUBLIC_*` вшиваются при сборке → после смены нужен передеплой. Счётчики только на сайте, в админке их нет |

⚠️ Не подтверждено, что пользователь внёс все 5 переменных (кроме DATABASE_URL) в Vercel — при проблемах на проде (логин/загрузка/телеграм) сначала проверить это.

⚠️ `AUTH_SECRET` и пароль администратора были пересозданы на этапе 6 (2026-07-09). Новое значение `AUTH_SECRET` пока только в локальном `.env` — чтобы логин на проде и локально использовал один и тот же секрет, это же значение нужно продублировать в Vercel (Settings → Environment Variables → AUTH_SECRET).

## Команды

- `npm run dev` / `npm run build`
- `npm run lint` / `npm run typecheck` / `npm test` — обязательная проверка перед коммитом
- `npm run db:push` — прогнать схему (идёт через DIRECT_URL)
- `npm run db:seed` — защищён guard'ом (см. `prisma/seed.ts`): откажется работать, если в базе уже есть данные, если не передан `SEED_FORCE=1`
- `npm run db:backup` / `npm run db:restore -- <файл>` — бэкап/восстановление всех таблиц в JSON (`backups/`, вне git)
- `npx tsx scripts/create-admin.ts <логин> <пароль> [имя]` — сменить пароль/создать админа
- Node 26 стоит в Program Files; агентским шеллам может понадобиться prepend PATH

## Статус (на 2026-07-09)

План админки, утверждённый пользователем, выполнен на 6/6 этапов:

1. ✅ Авторизация + каркас админки — `bfc14b5`
2. ✅ Форма заявки + Telegram + /admin/requests — `8c9a239` (телеграм-бота для просмотра заявок пользователь делает сам)
3. ✅ Медиа-хранилище Supabase Storage — `6954bd6` + фикс загрузки `35792f4`
4. ✅ CRUD туров — `828b836`
5. ✅ CRUD галереи/отзывов/блога/достижений — `50838d8` (+ подключение Achievements к БД, см. паттерн №4)
6. ✅ **Этап 6 — безопасная передача**: guard в `seed.ts`, скрипты `backup.ts`/`restore.ts`, новый `AUTH_SECRET` и финальный пароль админа (сгенерированы и переданы пользователю в чате, не в репозитории), `HANDOFF.md` с инструкцией для клиента и техническими заметками (в т.ч. рекомендация сменить `SUPABASE_SERVICE_KEY`/токен Telegram/пароль БД — они были засвечены в переписке с ассистентом на более ранних этапах)
7. 🔶 **Оптимизация**: Lighthouse mobile на проде 96/100/100/100 (коммит `9bf05c5`): отложенная загрузка скрытых hero-слайдов, touch-targets ≥24px у точек слайдеров, контраст футера, AVIF+кэш в next/image. **Языки вынесены в URL — сделано** (`4716fb2`, см. раздел выше). Аналитика — сделано (`d6f1d6d`), ждёт ID от пользователя.
8. ✅ **Разбор внешнего ревью** (2026-07-15): починены `featured` (не доезжал до главной), пустая таблица → демо, обязательный email в форме заявки (терялись лиды с телефоном), JSON-LD клеил `SITE_URL` к абсолютным Supabase-URL (`absoluteUrl()` в `seo.ts`), одноразовый токен Turnstile не сбрасывался после ошибки (форма умирала), лимит загрузки 8 МБ при потолке Vercel 4,5 МБ → 4 МБ.

**Долги из внешнего ревью — статус на 2026-07-15:**
- ✅ `<html lang>` исправлен несколькими root-layout: публичный layout получает реальный `[lang]`, админка использует `ru`, общие шрифты/тема вынесены в `RootHtml`.
- ✅ Лимиты логина, заявок и отзывов перенесены из локальных `Map` в атомарный Postgres `RateLimit`; логин ограничен по IP, поэтому знание имени администратора не позволяет заблокировать его для всех посетителей.
- ✅ Переименование/удаление города теперь в одной транзакции переносит/очищает `Tour.city`.
- ✅ MIME загрузок проверяется по magic bytes (JPEG/PNG/WebP/AVIF/MP4), а не по словам клиента; лимит 4 МБ.
- ✅ Добавлены ESLint, TypeScript check и 13 node-тестов для i18n/SEO/file signatures.
- ✅ Если запись отзыва в БД падает после успешной загрузки фото, endpoint удаляет новый объект из Storage; отсутствующий/нецелый рейтинг больше не превращается молча в 1.

9. ✅ **Единая память проекта** (2026-07-15): по решению пользователя удалён дублирующий `AGENTS.md`. Для совместной поочерёдной работы Claude и ChatGPT используется только корневой `CLAUDE.md`; правило закреплено в начале этого файла.

10. ✅ **Безопасность и целостность** (2026-07-15, незавершённая работа Claude доведена Codex): распределённый rate limit для логина/лидов/отзывов, проверка сигнатур загружаемых файлов, очистка Turnstile widget/token, удаление фото отзыва при откате DB-записи, строгая проверка рейтинга, транзакционная синхронизация direction slug с турами, корректные root-layout'ы и `<html lang>`, ESLint + typecheck + 13 тестов. Таблица `RateLimit` проверена в рабочем Supabase; `npm audit --omit=dev` — 0 уязвимостей.

11. ✅ **Быстрые переходы и кэш** (2026-07-15): причина секундных переходов была не только в расстоянии до БД. Корневой `not-found.tsx` вызывал `headers()` и переводил все публичные страницы в dynamic SSR; на проде это давало `private, no-cache, no-store` и `X-Vercel-Cache: MISS`. Убрана динамическая зависимость 404, каталог получил 5-минутный tagged cache, админка — `updateTag()` для всех локалей, Function закреплена в `fra1` рядом с Supabase Frankfurt (`vercel.json`). `next build --debug`: 39 публичных локализованных страниц SSG/ISR, только admin/API динамические. Локальный production: первый `/ru` ~56 мс, тёплые ответы 4–8 мс, `/ru/tours` отдаёт `x-nextjs-cache: HIT` и `s-maxage=300`. После деплоя `d9923cd` боевой `/ru/tours` отдаёт `X-Vercel-Cache: HIT`, `Age` растёт, `X-Vercel-Id` содержит только `fra1` (старого выполнения в `iad1` больше нет); один Node keep-alive сеанс дал первый запрос 734 мс и повторные 238–552 мс. В браузере проверены главная → туры и локализованная английская 404. Поэтому перенос сайта и БД на один сервер пока не нужен.

12. 🔎 **Аудит SEO/accessibility/performance без правок** (2026-07-16): свежий Lighthouse mobile на боевом `/ru` — Performance 85, Accessibility 100, Best Practices 100, SEO 100; FCP 2,3 с, LCP 3,2 с, TBT 30 мс, CLS 0, Speed Index 6,6 с. База SEO уже сделана: canonical + hreflang/x-default, sitemap/robots, локальные metadata/Open Graph, JSON-LD `TravelAgency`, `TouristTrip`, `BlogPosting`, корректный `<html lang>`. Следующий SEO-слой: видимые breadcrumbs + `BreadcrumbList` на турах/статьях, реальные `lastModified` в sitemap, локализованные Twitter/Open Graph locale и подключение Google Search Console/Яндекс Вебмастера после получения verification ID. Главный программный резерв скорости: почти все публичные page-компоненты сейчас `use client`; постепенно оставить клиентскими только интерактивные островки и передавать локализованный текст сервером. Hero через Next Image уже отдаётся в AVIF (~53 КБ для 750 px), поэтому слепое пережатие исходного JPG не главный выигрыш. Ручные accessibility-долги, которые Lighthouse не ловит: skip-link; desktop submenu туров должен открываться с клавиатуры/focus, не только hover; mobile-menu button имеет неверный `aria-label` (название главной вместо меню); LangSwitcher нужны `aria-label`/Escape/focus handling; Lightbox нужны focus trap/возврат фокуса/`aria-labelledby`, локализованные Close/Previous/Next. `prefers-reduced-motion` уже реализован в globals.css.

13. ✅ **SEO-разметка и клавиатурная доступность** (2026-07-16): добавлены локализованный skip-link с реальным переносом фокуса, корректные `aria-label` темы/мобильного меню/языка, `aria-expanded`/`aria-controls`/`inert`, Escape и возврат фокуса для меню языка, доступное с клавиатуры desktop-подменю туров. Lightbox теперь получает начальный фокус, удерживает Tab/Shift+Tab внутри, закрывается Escape, возвращает фокус на открывшее фото, имеет `aria-labelledby` и локализованные Close/Previous/Next; те же подписи локализованы у слайдеров. На турах и статьях видны breadcrumbs, рядом выдаётся валидный `BreadcrumbList`; helper покрыт тестом. `sitemap.xml` получает `Tour.updatedAt` и дату публикации статей (`lastmod` присутствует у 30 локализованных URL при текущих 6 турах/4 статьях). Проверено: production build сохраняет все публичные страницы SSG/ISR; lint без ошибок (3 прежних warnings), typecheck, 14/14 тестов; локальный Lighthouse Accessibility/SEO/Best Practices = 100/100/100 без failed binary audits. В браузере вручную проверены skip-link, Tab-порядок submenu, Escape языка, breadcrumbs/JSON-LD и полный focus cycle lightbox.

Эталонные данные прода после этапа 5: 6 туров, 9 фото, 3 отзыва, 4 статьи, 0 достижений.
См. также [HANDOFF.md](HANDOFF.md) — инструкция для владельца сайта и технического сопровождения.
